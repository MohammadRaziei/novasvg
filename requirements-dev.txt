# build dependencies
scikit-build-core
nanobind
numpy

# development dependencies
build
twine

# testing dependencies
pytest
pytest-cov

# documentation dependencies (novasvg uses plain Doxygen, not Sphinx --
# doxygen/graphviz themselves are installed via apt in cmake.yml, not here)
